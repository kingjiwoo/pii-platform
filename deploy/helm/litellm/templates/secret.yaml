apiVersion: v1
kind: Secret
metadata:
  name: {{ include "litellm.fullname" . }}-secrets
  labels:
    {{- include "litellm.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
