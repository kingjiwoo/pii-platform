# LLM 백엔드 API keys (실제 값은 .env에, .env는 gitignored)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# LiteLLM Gateway 접근 인증 (모든 요청이 이 키로 게이트웨이 통과)
LITELLM_MASTER_KEY=

# 로컬 dev 오버라이드 (K8s 배포 시엔 in-cluster DNS 사용)
# LITELLM_URL=http://localhost:4000
